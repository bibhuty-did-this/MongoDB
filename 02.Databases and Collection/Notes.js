/**
 * Databases and Collections:
 *  + MongoDB stores BSON documents, i.e. data records, in collections; the collections in database.
 *  + In MongoDB, database holds collection of documents.
 *  + MongoDB stores documents in collections. Collections are analogous to tables in relational database.
 */
